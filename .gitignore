build_*/
.deps/
.cache/
.vs/
.vscode/
vcpkg_installed/
CMakeUserPresets.json
*.user

# Generated install payload (rebuild with: cmake --install ... --prefix package).
# The helper scripts/notes in package/ are kept; only the built tree is ignored.
/package/font-effect-tools/

# Release staging / distributable archives (attached to GitHub Releases, not tracked).
/release/
*.dll
*.pdb
*.exp
*.lib
